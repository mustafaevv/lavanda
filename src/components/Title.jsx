import styled from "styled-components";

const TitleStyled = styled.h2`
  text-align: center;
  font-size: 34px;
  color: #000;
  font-weight: bold;
  margin-top: 20px;
`;

const Title = () => {
  return <TitleStyled>Empty</TitleStyled>;
};

export default Title;
